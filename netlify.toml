[build]
  command = "npm run build"
  publish = "dist/color-lock-web"

[dev]
  command = "npm run dev"
  port = 3000

# Handle SPA routing
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200 